# Optional. Board manual registration: same UI with ?manual=1 posts rows directly to Google Sheets.
# Use a long random string. Set in Netlify (or .env.local). If unset/empty, the admin route returns 404.
REGISTRATION_ADMIN_SECRET=
